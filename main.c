#include "euclidean.h"

int main(void) {
    //Algorithm
    /*
    int a, b, q, r, numerator, denominator;    

    a = 340;
    b = 45;

    while(b >= 0){
        q = a / b;
        r = a - (b * q);

        a = b;
        b = r;
    }

    numerator = numerator / a;
    denominator = denominator / a;
    */
    return EXIT_SUCCESS;
}
